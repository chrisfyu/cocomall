spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://mysql-master.cocomall:3306/cocomall_wms
    driver-class-name: com.mysql.jdbc.Driver

  jackson:
    time-zone: Asia/Tokyo
    date-format: yyyy-MM-dd HH:mm:ss

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 8080

logging:
  level:
    com.atguigu: debug